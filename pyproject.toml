[project]
name = "genlayer-dispute-escrow-demo"
version = "0.1.0"
description = "Dispute escrow MVP sketch for GenLayer Intelligent Contracts"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
